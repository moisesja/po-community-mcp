# Task: Enable restrained logging for the .NET MCP server

## Plan

- [x] Inspect the current .NET logging setup and identify the best insertion point.
- [x] Add structured logging at the shared MCP handler layer for tool listing and tool execution.
- [x] Keep logs privacy-safe and low-noise by avoiding raw patient data and logging details.
- [x] Build the changed .NET projects to verify the change compiles cleanly.
- [x] Update the review notes and changelog after validation.

## Review

- Added shared `Po.Community.Mcp` logging for tool execution outcomes in `McpClientCallToolService`.
- Logged tool discovery at `Debug` in `McpClientListToolsService` so normal runs stay quieter.
- Avoided logging raw tool arguments or patient data.
- Validation:
  - `dotnet build dotnet/Po.Community.Core/Po.Community.Core.csproj --no-restore -nologo -v quiet`
  - `dotnet build dotnet/Po.Community.Server/Po.Community.Server.csproj --no-restore -nologo -v quiet`
